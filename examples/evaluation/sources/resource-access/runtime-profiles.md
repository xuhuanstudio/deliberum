# Runtime Profile Source Excerpt

Sample fixture material for safe runtime profile metadata.

- Reports whether resource access base URL, signing, and TTL are configured.
- Reports exposure class and production-readiness blockers without printing configured URLs.
- Keeps signing secrets, access ids, redirect targets, hosted content, and payloads out of public profile responses.
- Exposes enough non-secret setup state for CLI and Web posture views to explain required operator actions.
