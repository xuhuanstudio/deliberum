# Direct Answer Baseline

Summary used by the public evaluation fixture.

The direct response recommended making resource URLs configurable and mentioned HTTPS for public deployments. It did not make non-local exposure an explicit opt-in boundary, did not separate access-layer bearer material from semantic events, and did not describe a grant lifecycle audit trail.
