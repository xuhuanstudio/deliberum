# Multi-Perspective Baseline

Summary used by the public evaluation fixture.

The response requested multiple perspectives in one provider call. It improved breadth over a direct response while keeping low latency, but did not expose separate lifecycle events or an auditable projection trail.
