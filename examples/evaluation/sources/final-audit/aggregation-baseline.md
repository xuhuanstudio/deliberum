# Aggregation Baseline

Summary used by the public evaluation fixture.

Multiple candidate responses were aggregated into one result using simple preference counts. The result retained a rough consensus signal, but did not keep minority concerns, final audit findings, or unresolved evidence state as first-class material.
